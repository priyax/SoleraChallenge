# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FoodCurrencyConvertor' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FoodCurrencyConvertor

  target 'FoodCurrencyConvertorTests' do
    inherit! :search_paths
    # Pods for testing
   
   pod 'Alamofire', '~> 4.5’ 
   pod 'SwiftyJSON'
  end

end
